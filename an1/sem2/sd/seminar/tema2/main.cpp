#include <iostream>
#include <fstream>
#include <cstring>
#include <stack>

using namespace std;
// cerinta: https://infoarena.ro/problema/evaluare

int solve(const string& input) {
    int rez = 0;
    stack<char> op, nums;
    for (int i = 0; i < input.length(); i++) {
        if ()
    }

    return rez;
}

int main() {
    ifstream f("evaluare.in");
    ofstream g("evaluare.out");
    string input;
    f >> input;
    g << solve(input);

    return 0;
}
